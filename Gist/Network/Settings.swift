struct Settings {
    struct Production {
        static let analyticsAPI = "https://analytics.api.gist.build"
        static let queueAPI = "https://queue.api.gist.build"
        static let gistAPI = "https://api.gist.build"
    }
}
